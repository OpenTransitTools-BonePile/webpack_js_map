{
    "name": "map",
    "version": "0.0.1",
    "description": "map",
    "main": "index.js",
    "scripts": {
        "prestart": "yarn",
        "start": "webpack-dev-server",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "react": "^15.4.2",
        "yaml-loader": "^0.4.0"
    }
}
